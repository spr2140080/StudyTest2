<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studytest2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyTest2">
        <activity android:name=".SubjectActivity3"></activity>
        <activity android:name=".ConcentrationActivity" />
        <activity android:name=".EfficiencyActivity" />
        <activity android:name=".RestActivity" />
        <activity android:name=".SubjectActivity2" />
        <activity
            android:name=".TimeActivity"
            android:label="@string/title_activity_time"
            android:theme="@style/Theme.StudyTest2.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>